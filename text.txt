TO DO:
- Make it so the quote changes every time a new tab is created. Should be same as the background image
- Add the author of the quote.



const quotesApi = "https://quotes.rest/qod?language=en"

 console.log(quotesApi)

 fetch(quotesApi)
   .then(response =>{
        return response.json();
    })
    .then(data => {
        const quoteText = data.contents.quotes[0].quote
        $("#quote").text(quoteText)
        console.log(quotesText)
    })

// 123C50!@#